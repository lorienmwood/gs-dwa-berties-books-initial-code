<!DOCTYPE html>
<html>

<head>
    <title>
        <%=shopData.shopName%> Check the Weather
    </title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
</head>

<body>
    <div class="container">
        <h1>
            <%=shopData.shopName%> Check the Weather
        </h1>

        <form method="get" action="/weather">
            <label for="city">Enter city name:</label><br>
            <br>
            <input type="text" id="city" name="city" placeholder="e.g. London" />
            <button type="submit">Submit</button>
        </form>

        <% if (error) { %>
            <div class="error">
                <%= error %>
            </div>
            <% } %>

                <% if (weather) { %>
                    <div class="weather-box">

                        <h2 class="weather-title">
                            <%= weather.name %>  Weather
                        </h2>

                        <ul class="weather-list">
                            <li><span class="label">Temperature:</span>
                                <%= weather.temp %> °C
                            </li>
                            <li><span class="label">Humidity:</span>
                                <%= weather.humidity %> %
                            </li>

                            <% if (weather.windSpeed !==undefined) { %>
                                <li><span class="label">Wind Speed:</span>
                                    <%= weather.windSpeed %> m/s
                                </li>
                                <% } %>

                                    <% if (weather.description) { %>
                                        <li><span class="label">Conditions:</span>
                                            <%= weather.description %>
                                        </li>
                                        <% } %>
                        </ul>

                    </div>
                    <% } %>
    </div>
</body>

</html>